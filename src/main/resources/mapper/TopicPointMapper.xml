<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bing.tpa.mapper.TopicPointMapper">

    <select id="findByTsIdAndSchoolId" resultType="com.bing.tpa.domain.entity.TopicPoint">
        SELECT * FROM topic_points 
        WHERE ts_id = #{tsId} 
        AND school_id = #{schoolId}
        ORDER BY level, tss_id
    </select>

    <select id="findByTemplateId" resultType="com.bing.tpa.domain.entity.TopicPoint">
        SELECT * FROM topic_points 
        WHERE template_id = #{templateId}
    </select>

    <!-- 根据schoolId查询知识点 -->
    <select id="findBySchoolId" resultType="com.bing.tpa.domain.entity.TopicPoint">
        SELECT * FROM topic_points
        WHERE school_id = #{schoolId}
    </select>

    <select id="findByLevel" resultType="com.bing.tpa.domain.entity.TopicPoint">
        SELECT * FROM topic_points 
        WHERE ts_id = #{tsId}
        AND school_id = #{schoolId}
        AND level = #{level}
    </select>
    <!-- 根据tsId查询知识点 -->
    <select id="findByTsId" resultType="com.bing.tpa.domain.entity.TopicPoint">
        SELECT * FROM topic_points
        WHERE ts_id = #{tsId}
    </select>
    <select id="findChapterByTitle" resultType="java.lang.Integer">
        SELECT tss_id FROM topic_points
        WHERE school_id = #{schoolId}
          AND ts_id = #{tsId}
          AND top_title = #{title}
          AND level = #{level}
        LIMIT 1
    </select>
    <select id="findByTitleAndLevel" resultType="java.lang.Integer">
        SELECT tss_id FROM topic_points
        WHERE school_id = #{schoolId}
          AND ts_id = #{tsId}
          AND top_title = #{title}
          AND level = #{level}
        LIMIT 1
    </select>
    <select id="findByTemplateId1" resultType="com.bing.tpa.domain.entity.TopicPoint">
        SELECT * FROM topic_points
        WHERE school_id = #{schoolId}
          AND ts_id = #{tsId}
          AND template_id = #{templateId}
        LIMIT 1
    </select>
    <select id="selectIds1" resultType="java.lang.Integer">
        select tss_id from topic_points
        where top_title=(
            select top_title from topic_points
            where tss_id= #{tssId}
            LIMIT 1
            )
    </select>
    <select id="selectUnderTow" resultType="com.bing.tpa.domain.entity.TopicPoint">
        select tss_id,top_title,level,secondary_title from topic_points
        where school_id= #{schoolId}
        and ts_id= #{tsId}
    </select>
    <select id="getMaxLevel" resultType="java.lang.Integer">
            select max(level) from topic_points where school_id= #{schoolId}
            and ts_id= #{tsId}
    </select>
    <select id="getLevelPoints" resultType="com.bing.tpa.domain.entity.TopicPoint">
            select tss_id,top_title,level,secondary_title from topic_points
            where school_id= #{schoolId}
            and ts_id= #{tsId}
            and level= #{level}
    </select>
    <select id="getChildByParentId" resultType="com.bing.tpa.domain.entity.TopicPoint"
            parameterType="java.lang.Integer">
        SELECT
        tp.tss_id,
        tp.top_title,
        tp.level,
        tp.secondary_title,
        tr.relation_type
        FROM topic_points tp
        INNER JOIN topic_relations tr ON tp.tss_id = tr.child_tss_id
        WHERE tr.parent_tss_id = #{tssId}
    </select>


    <insert id="insertBatch">
        INSERT INTO topic_points (ts_id, school_id, template_id, top_title, secondary_title, 
                                 defined_title, content, level, is_template, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.tsId}, #{item.schoolId}, #{item.templateId}, #{item.topTitle}, 
             #{item.secondaryTitle}, #{item.definedTitle}, #{item.content}, 
             #{item.level}, #{item.isTemplate}, NOW(), NOW())
        </foreach>
    </insert>

    <!-- 批量删除知识点 -->
    <delete id="batchDeleteByIds">
        DELETE FROM topic_points
        WHERE tss_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>

    </delete>

    <!-- 根据知识点ID删除相关关系 -->
    <delete id="deleteByPointIds">
        DELETE FROM topic_relations
        WHERE
        <choose>
            <when test="ids != null and ids.size() > 0">
                parent_tss_id IN
                <foreach collection="ids" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
                OR child_tss_id IN
                <foreach collection="ids" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </when>
            <otherwise>
                1 = 0
            </otherwise>
        </choose>
    </delete>

</mapper>
    